anthropic>=0.40.0
pydantic>=2.7
python-dotenv>=1.0
typer>=0.12
rich>=13.7
pandas>=2.2
beautifulsoup4>=4.12
lxml>=5.2

# Retrieval (semantic fallback embeddings)
sentence-transformers>=3.0
numpy>=1.26

# API / live demo
fastapi>=0.111
uvicorn>=0.30
psycopg2-binary>=2.9   # Postgres driver, prod-only (DATABASE_URL) -- see db.py
slowapi>=0.1.9         # per-IP rate limiting on the public check/codes endpoints

# dev
pytest>=8.2
